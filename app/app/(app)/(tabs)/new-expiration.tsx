import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function NewExpiration() {
    return (
        <SafeAreaView>
            <Text>new expiration</Text>
        </SafeAreaView>
    );
}
